const initialWidth: number = 500;
const initialHeight: number = 300;

export default {
  MINIMAL_SIZE: 200,
  INITIAL_WIDTH: initialWidth,
  INITIAL_HEIGHT: initialHeight,
  INITIAL_LEFT: window.innerWidth / 2 - initialWidth / 2,
  INITIAL_TOP: window.innerHeight / 2 - initialHeight / 2,
  INITIAL_MINIMALIZED: false
};
