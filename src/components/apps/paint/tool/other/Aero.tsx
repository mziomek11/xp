import React from "react";

import Tool from "../Tool";
import aeroIcon from "../../../../../assets/paint/aero.png";

const Aero = () => {
  return <Tool icon={aeroIcon} toolType="aero" data-test="tool" />;
};

export default Aero;
